[1mdiff --git a/realtime_machine_nowebsite/agent.py b/realtime_machine_nowebsite/agent.py[m
[1mindex b75ff7b..4935a31 100644[m
[1m--- a/realtime_machine_nowebsite/agent.py[m
[1m+++ b/realtime_machine_nowebsite/agent.py[m
[36m@@ -4,6 +4,7 @@[m [mfrom pymongo import MongoClient[m
 import sys[m
 import hashlib[m
 import requests[m
[32m+[m[32mfrom collections import defaultdict[m
 import os[m
 import threading[m
 from scapy.all import sniff, IP, TCP, UDP[m
[36m@@ -13,6 +14,7 @@[m [mimport getpass[m
 import argparse[m
 [m
 [m
[32m+[m
 # --- CONFIGURATION ---[m
 DEFAULT_PATHS = ["/etc/nginx", "/var/www/html"] [m
 CHECK_INTERVAL = 10 [m
[36m@@ -23,6 +25,23 @@[m [mSIEM_DB_URL = ""[m
 NETWORK_BACKEND_URL = ""[m
 CONFIG_URL = ""[m
 [m
[32m+[m[32m# ─────────────────────────────────────────────────────────────────────────────[m
[32m+[m[32m# NEW FEATURE: Per-IP tracking windows (for connections_per_ip_window,[m
[32m+[m[32m#              unique_ports_per_ip, failed_connection_ratio)[m
[32m+[m[32m# ─────────────────────────────────────────────────────────────────────────────[m
[32m+[m[32mIP_WINDOW_SECONDS = 60          # sliding window width (seconds)[m
[32m+[m[32mip_conn_times      = defaultdict(list)   # src_ip -> [timestamps of connections][m
[32m+[m[32mip_ports_seen      = defaultdict(set)    # src_ip -> {dst_ports contacted}[m
[32m+[m[32mip_failed_counts   = defaultdict(int)    # src_ip -> count of failed (INT/RST) flows[m
[32m+[m[32mip_total_counts    = defaultdict(int)    # src_ip -> total flows initiated[m
[32m+[m[32mip_lock            = threading.Lock()    # thread-safety for the dicts above[m
[32m+[m
[32m+[m[32mdef _prune_window(src_ip, now_ts):[m
[32m+[m[32m    """Remove connection timestamps outside the sliding window for src_ip."""[m
[32m+[m[32m    cutoff = now_ts - IP_WINDOW_SECONDS[m
[32m+[m[32m    ip_conn_times[src_ip] = [t for t in ip_conn_times[src_ip] if t >= cutoff][m
[32m+[m
[32m+[m
 def check_permissions():[m
     # Check if we are root[m
     if os.geteuid() == 0:[m
[36m@@ -176,6 +195,9 @@[m [mdef process_packet(packet):[m
         elif session["syn_count"] > 0 and session["dpkts"] == 0:[m
             session["state"] = "INT" # Interrupted / No response (common in SYN floods)[m
 [m
[32m+[m[32m        if session["state"] in ("INT", "RST"):[m
[32m+[m[32m            with ip_lock:[m
[32m+[m[32m                ip_failed_counts[session["src_ip"]] +=1[m
         # Calculate IAT (Inter-Arrival Times)[m
         iat_list = [t2 - t1 for t1, t2 in zip(session["timestamps"], session["timestamps"][1:])][m
         mean_iat = statistics.mean(iat_list) if iat_list else 0[m
[36m@@ -191,6 +213,53 @@[m [mdef process_packet(packet):[m
         elif session["src_port"] in SERVICE_MAP:[m
             service = SERVICE_MAP.get(session["src_port"], "-")[m
 [m
[32m+[m[32m        # ── NEW FEATURES ─────────────────────────────────────────────────────[m
[32m+[m
[32m+[m[32m        # 1. flow_bytes – total bytes exchanged in this flow (sbytes + dbytes)[m
[32m+[m[32m        flow_bytes = total_bytes[m
[32m+[m
[32m+[m[32m        # 2. bytes_per_pkt – average bytes per packet across the whole flow[m
[32m+[m[32m        bytes_per_pkt = (total_bytes / total_pkts) if total_pkts > 0 else 0[m
[32m+[m
[32m+[m[32m        # 3. syn_ratio – fraction of packets that carried a SYN flag[m
[32m+[m[32m        #    High values (near 1.0) indicate SYN-flood style behaviour[m
[32m+[m[32m        syn_ratio = (session["syn_count"] / total_pkts) if total_pkts > 0 else 0[m
[32m+[m
[32m+[m[32m        # 4. ack_ratio – fraction of packets that carried an ACK flag[m
[32m+[m[32m        #    Normal established flows hover around 1.0; very low values are suspicious[m
[32m+[m[32m        ack_ratio = (session["ack_count"] / total_pkts) if total_pkts > 0 else 0[m
[32m+[m
[32m+[m[32m        # 5. rst_ratio – fraction of packets that carried a RST flag[m
[32m+[m[32m        #    Elevated values suggest port-scanning or aggressive connection teardown[m
[32m+[m[32m        rst_ratio = (session["rst_count"] / total_pkts) if total_pkts > 0 else 0[m
[32m+[m
[32m+[m[32m        # 6. iat_ratio – coefficient of variation of inter-arrival times (std / mean)[m
[32m+[m[32m        #    High value → bursty / irregular traffic; low value → steady stream[m
[32m+[m[32m        iat_ratio = (std_iat / mean_iat) if mean_iat > 0 else 0[m
[32m+[m
[32m+[m[32m        # 7–9  Per-IP window metrics (thread-safe snapshot)[m
[32m+[m[32m        flow_src_ip = session["src_ip"][m
[32m+[m[32m        now_ts = now.timestamp()[m
[32m+[m[32m        with ip_lock:[m
[32m+[m[32m            _prune_window(flow_src_ip, now_ts)[m
[32m+[m
[32m+[m[32m            # 7. unique_ports_per_ip – distinct dst ports this src IP has[m
[32m+[m[32m            #    contacted since agent start (port-scan indicator)[m
[32m+[m[32m            unique_ports_per_ip = len(ip_ports_seen[flow_src_ip])[m
[32m+[m
[32m+[m[32m            # 8. connections_per_ip_window – how many connections this src IP[m
[32m+[m[32m            #    has opened inside the last IP_WINDOW_SECONDS seconds[m
[32m+[m[32m            connections_per_ip_window = len(ip_conn_times[flow_src_ip])[m
[32m+[m
[32m+[m[32m            # 9. failed_connection_ratio – ratio of failed (INT / RST) flows[m
[32m+[m[32m            #    to total flows for this src IP since agent start[m
[32m+[m[32m            total_for_ip  = ip_total_counts[flow_src_ip][m
[32m+[m[32m            failed_for_ip = ip_failed_counts[flow_src_ip][m
[32m+[m[32m            failed_connection_ratio = (failed_for_ip / total_for_ip) if total_for_ip > 0 else 0[m
[32m+[m
[32m+[m[32m        # ─────────────────────────────────────────────────────────────────────[m
[32m+[m
[32m+[m
         final_log = {[m
             "hostname": session["hostname"],[m
             "timestamp": now.isoformat(),[m
[36m@@ -205,7 +274,7 @@[m [mdef process_packet(packet):[m
             "sttl": session["sttl"], "dttl": session["dttl"],[m
             "synack": session["synack"], "ackdat": session["ackdat"],[m
             "stcpb": session["stcpb"], "dtcpb": session["dtcpb"],[m
[31m-            # Engineered Features[m
[32m+[m[32m            # Original Engineered Features[m
             "rate": (total_bytes / duration) if duration > 0 else 0,[m
             "pkt_rate": (total_pkts / duration) if duration > 0 else 0,[m
             "byte_rate": (total_bytes / duration) if duration > 0 else 0,[m
[36m@@ -217,7 +286,18 @@[m [mdef process_packet(packet):[m
             "mean_iat": mean_iat,[m
             "std_iat": std_iat,[m
             "flow_packets": total_pkts,[m
[31m-            "port_count": len(session["ports_seen"]), # Usually 1 per flow unless tracking source wide[m
[32m+[m[32m            "port_count": len(session["ports_seen"]),[m
[32m+[m[32m            # ── NEW Features ──────────────────────────────────────────────────[m
[32m+[m[32m            "flow_bytes": flow_bytes,                                # total bytes in flow[m
[32m+[m[32m            "bytes_per_pkt": bytes_per_pkt,                          # avg bytes / packet[m
[32m+[m[32m            "syn_ratio": syn_ratio,                                  # SYN pkts / total pkts[m
[32m+[m[32m            "ack_ratio": ack_ratio,                                  # ACK pkts / total pkts[m
[32m+[m[32m            "rst_ratio": rst_ratio,                                  # RST pkts / total pkts[m
[32m+[m[32m            "iat_ratio": iat_ratio,                                  # std_iat / mean_iat (CoV)[m
[32m+[m[32m            "unique_ports_per_ip": unique_ports_per_ip,              # distinct dst ports (src IP)[m
[32m+[m[32m            "connections_per_ip_window": connections_per_ip_window,  # conns in sliding window[m
[32m+[m[32m            "failed_connection_ratio": failed_connection_ratio,      # failed / total (src IP)[m
[32m+[m[32m            # ─────────────────────────────────────────────────────────────────[m
             "processed": False[m
         }[m
 [m
[1mdiff --git a/realtime_website/agent.py b/realtime_website/agent.py[m
[1mindex b75ff7b..4935a31 100644[m
[1m--- a/realtime_website/agent.py[m
[1m+++ b/realtime_website/agent.py[m
[36m@@ -4,6 +4,7 @@[m [mfrom pymongo import MongoClient[m
 import sys[m
 import hashlib[m
 import requests[m
[32m+[m[32mfrom collections import defaultdict[m
 import os[m
 import threading[m
 from scapy.all import sniff, IP, TCP, UDP[m
[36m@@ -13,6 +14,7 @@[m [mimport getpass[m
 import argparse[m
 [m
 [m
[32m+[m
 # --- CONFIGURATION ---[m
 DEFAULT_PATHS = ["/etc/nginx", "/var/www/html"] [m
 CHECK_INTERVAL = 10 [m
[36m@@ -23,6 +25,23 @@[m [mSIEM_DB_URL = ""[m
 NETWORK_BACKEND_URL = ""[m
 CONFIG_URL = ""[m
 [m
[32m+[m[32m# ─────────────────────────────────────────────────────────────────────────────[m
[32m+[m[32m# NEW FEATURE: Per-IP tracking windows (for connections_per_ip_window,[m
[32m+[m[32m#              unique_ports_per_ip, failed_connection_ratio)[m
[32m+[m[32m# ─────────────────────────────────────────────────────────────────────────────[m
[32m+[m[32mIP_WINDOW_SECONDS = 60          # sliding window width (seconds)[m
[32m+[m[32mip_conn_times      = defaultdict(list)   # src_ip -> [timestamps of connections][m
[32m+[m[32mip_ports_seen      = defaultdict(set)    # src_ip -> {dst_ports contacted}[m
[32m+[m[32mip_failed_counts   = defaultdict(int)    # src_ip -> count of failed (INT/RST) flows[m
[32m+[m[32mip_total_counts    = defaultdict(int)    # src_ip -> total flows initiated[m
[32m+[m[32mip_lock            = threading.Lock()    # thread-safety for the dicts above[m
[32m+[m
[32m+[m[32mdef _prune_window(src_ip, now_ts):[m
[32m+[m[32m    """Remove connection timestamps outside the sliding window for src_ip."""[m
[32m+[m[32m    cutoff = now_ts - IP_WINDOW_SECONDS[m
[32m+[m[32m    ip_conn_times[src_ip] = [t for t in ip_conn_times[src_ip] if t >= cutoff][m
[32m+[m
[32m+[m
 def check_permissions():[m
     # Check if we are root[m
     if os.geteuid() == 0:[m
[36m@@ -176,6 +195,9 @@[m [mdef process_packet(packet):[m
         elif session["syn_count"] > 0 and session["dpkts"] == 0:[m
             session["state"] = "INT" # Interrupted / No response (common in SYN floods)[m
 [m
[32m+[m[32m        if session["state"] in ("INT", "RST"):[m
[32m+[m[32m            with ip_lock:[m
[32m+[m[32m                ip_failed_counts[session["src_ip"]] +=1[m
         # Calculate IAT (Inter-Arrival Times)[m
         iat_list = [t2 - t1 for t1, t2 in zip(session["timestamps"], session["timestamps"][1:])][m
         mean_iat = statistics.mean(iat_list) if iat_list else 0[m
[36m@@ -191,6 +213,53 @@[m [mdef process_packet(packet):[m
         elif session["src_port"] in SERVICE_MAP:[m
             service = SERVICE_MAP.get(session["src_port"], "-")[m
 [m
[32m+[m[32m        # ── NEW FEATURES ─────────────────────────────────────────────────────[m
[32m+[m
[32m+[m[32m        # 1. flow_bytes – total bytes exchanged in this flow (sbytes + dbytes)[m
[32m+[m[32m        flow_bytes = total_bytes[m
[32m+[m
[32m+[m[32m        # 2. bytes_per_pkt – average bytes per packet across the whole flow[m
[32m+[m[32m        bytes_per_pkt = (total_bytes / total_pkts) if total_pkts > 0 else 0[m
[32m+[m
[32m+[m[32m        # 3. syn_ratio – fraction of packets that carried a SYN flag[m
[32m+[m[32m        #    High values (near 1.0) indicate SYN-flood style behaviour[m
[32m+[m[32m        syn_ratio = (session["syn_count"] / total_pkts) if total_pkts > 0 else 0[m
[32m+[m
[32m+[m[32m        # 4. ack_ratio – fraction of packets that carried an ACK flag[m
[32m+[m[32m        #    Normal established flows hover around 1.0; very low values are suspicious[m
[32m+[m[32m        ack_ratio = (session["ack_count"] / total_pkts) if total_pkts > 0 else 0[m
[32m+[m
[32m+[m[32m        # 5. rst_ratio – fraction of packets that carried a RST flag[m
[32m+[m[32m        #    Elevated values suggest port-scanning or aggressive connection teardown[m
[32m+[m[32m        rst_ratio = (session["rst_count"] / total_pkts) if total_pkts > 0 else 0[m
[32m+[m
[32m+[m[32m        # 6. iat_ratio – coefficient of variation of inter-arrival times (std / mean)[m
[32m+[m[32m        #    High value → bursty / irregular traffic; low value → steady stream[m
[32m+[m[32m        iat_ratio = (std_iat / mean_iat) if mean_iat > 0 else 0[m
[32m+[m
[32m+[m[32m        # 7–9  Per-IP window metrics (thread-safe snapshot)[m
[32m+[m[32m        flow_src_ip = session["src_ip"][m
[32m+[m[32m        now_ts = now.timestamp()[m
[32m+[m[32m        with ip_lock:[m
[32m+[m[32m            _prune_window(flow_src_ip, now_ts)[m
[32m+[m
[32m+[m[32m            # 7. unique_ports_per_ip – distinct dst ports this src IP has[m
[32m+[m[32m            #    contacted since agent start (port-scan indicator)[m
[32m+[m[32m            unique_ports_per_ip = len(ip_ports_seen[flow_src_ip])[m
[32m+[m
[32m+[m[32m            # 8. connections_per_ip_window – how many connections this src IP[m
[32m+[m[32m            #    has opened inside the last IP_WINDOW_SECONDS seconds[m
[32m+[m[32m            connections_per_ip_window = len(ip_conn_times[flow_src_ip])[m
[32m+[m
[32m+[m[32m            # 9. failed_connection_ratio – ratio of failed (INT / RST) flows[m
[32m+[m[32m            #    to total flows for this src IP since agent start[m
[32m+[m[32m            total_for_ip  = ip_total_counts[flow_src_ip][m
[32m+[m[32m            failed_for_ip = ip_failed_counts[flow_src_ip][m
[32m+[m[32m            failed_connection_ratio = (failed_for_ip / total_for_ip) if total_for_ip > 0 else 0[m
[32m+[m
[32m+[m[32m        # ─────────────────────────────────────────────────────────────────────[m
[32m+[m
[32m+[m
         final_log = {[m
             "hostname": session["hostname"],[m
             "timestamp": now.isoformat(),[m
[36m@@ -205,7 +274,7 @@[m [mdef process_packet(packet):[m
             "sttl": session["sttl"], "dttl": session["dttl"],[m
             "synack": session["synack"], "ackdat": session["ackdat"],[m
             "stcpb": session["stcpb"], "dtcpb": session["dtcpb"],[m
[31m-            # Engineered Features[m
[32m+[m[32m            # Original Engineered Features[m
             "rate": (total_bytes / duration) if duration > 0 else 0,[m
             "pkt_rate": (total_pkts / duration) if duration > 0 else 0,[m
             "byte_rate": (total_bytes / duration) if duration > 0 else 0,[m
[36m@@ -217,7 +286,18 @@[m [mdef process_packet(packet):[m
             "mean_iat": mean_iat,[m
             "std_iat": std_iat,[m
             "flow_packets": total_pkts,[m
[31m-            "port_count": len(session["ports_seen"]), # Usually 1 per flow unless tracking source wide[m
[32m+[m[32m            "port_count": len(session["ports_seen"]),[m
[32m+[m[32m            # ── NEW Features ──────────────────────────────────────────────────[m
[32m+[m[32m            "flow_bytes": flow_bytes,                                # total bytes in flow[m
[32m+[m[32m            "bytes_per_pkt": bytes_per_pkt,                          # avg bytes / packet[m
[32m+[m[32m            "syn_ratio": syn_ratio,                                  # SYN pkts / total pkts[m
[32m+[m[32m            "ack_ratio": ack_ratio,                                  # ACK pkts / total pkts[m
[32m+[m[32m            "rst_ratio": rst_ratio,                                  # RST pkts / total pkts[m
[32m+[m[32m            "iat_ratio": iat_ratio,                                  # std_iat / mean_iat (CoV)[m
[32m+[m[32m            "unique_ports_per_ip": unique_ports_per_ip,              # distinct dst ports (src IP)[m
[32m+[m[32m            "connections_per_ip_window": connections_per_ip_window,  # conns in sliding window[m
[32m+[m[32m            "failed_connection_ratio": failed_connection_ratio,      # failed / total (src IP)[m
[32m+[m[32m            # ─────────────────────────────────────────────────────────────────[m
             "processed": False[m
         }[m
 [m
[1mdiff --git a/windows_realtime/agent.py b/windows_realtime/agent.py[m
[1mindex 4b7cf95..761f793 100644[m
[1m--- a/windows_realtime/agent.py[m
[1m+++ b/windows_realtime/agent.py[m
[36m@@ -10,6 +10,7 @@[m [mimport winreg[m
 import threading[m
 from scapy.all import sniff, IP, TCP, UDP, ICMP[m
 from datetime import datetime, timezone[m
[32m+[m[32mfrom collections import defaultdict[m
 import argparse[m
 [m
 PROTOCOL_MAP = {6: "tcp", 17: "udp", 1: "icmp"}[m
[36m@@ -23,6 +24,24 @@[m [mCONFIG_URL = ""[m
 BACKEND_URL=""[m
 [m
 [m
[32m+[m[32m# ─────────────────────────────────────────────────────────────────────────────[m
[32m+[m[32m# NEW FEATURE: Per-IP tracking windows (for connections_per_ip_window,[m
[32m+[m[32m#              unique_ports_per_ip, failed_connection_ratio)[m
[32m+[m[32m# ─────────────────────────────────────────────────────────────────────────────[m
[32m+[m[32mIP_WINDOW_SECONDS = 60          # sliding window width (seconds)[m
[32m+[m[32mip_conn_times      = defaultdict(list)   # src_ip -> [timestamps of connections][m
[32m+[m[32mip_ports_seen      = defaultdict(set)    # src_ip -> {dst_ports contacted}[m
[32m+[m[32mip_failed_counts   = defaultdict(int)    # src_ip -> count of failed (INT/RST) flows[m
[32m+[m[32mip_total_counts    = defaultdict(int)    # src_ip -> total flows initiated[m
[32m+[m[32mip_lock            = threading.Lock()    # thread-safety for the dicts above[m
[32m+[m
[32m+[m
[32m+[m[32mdef _prune_window(src_ip, now_ts):[m
[32m+[m[32m    """Remove connection timestamps outside the sliding window for src_ip."""[m
[32m+[m[32m    cutoff = now_ts - IP_WINDOW_SECONDS[m
[32m+[m[32m    ip_conn_times[src_ip] = [t for t in ip_conn_times[src_ip] if t >= cutoff][m
[32m+[m
[32m+[m
 def is_admin():[m
     try:[m
         return ctypes.windll.shell32.IsUserAnAdmin()[m
[36m@@ -163,7 +182,7 @@[m [mdef process_packet(packet):[m
     src_ip = packet[IP].src[m
     dst_ip = packet[IP].dst[m
     proto_num = packet[IP].proto[m
[31m-    proto_str = PROTO_MAP.get(proto_num, "other")[m
[32m+[m[32m    proto_str = PROTOCOL_MAP.get(proto_num, "other")[m
     [m
     # Identify ports[m
     sport = packet[TCP].sport if packet.haslayer(TCP) else (packet[UDP].sport if packet.haslayer(UDP) else 0)[m
[36m@@ -189,12 +208,20 @@[m [mdef process_packet(packet):[m
             "synack_time": None,[m
             "synack": 0, "ackdat": 0,[m
             "state": "REQ",[m
[31m-            # New Tracking Fields[m
[32m+[m[32m            # Existing Tracking Fields[m
             "timestamps": [now.timestamp()],[m
             "syn_count": 0, "ack_count": 0, "fin_count": 0, "rst_count": 0,[m
             "ports_seen": {dport}[m
         }[m
 [m
[32m+[m[32m        # ── NEW: register this new connection in the per-IP window tracker ──[m
[32m+[m[32m        now_ts = now.timestamp()[m
[32m+[m[32m        with ip_lock:[m
[32m+[m[32m            _prune_window(src_ip, now_ts)[m
[32m+[m[32m            ip_conn_times[src_ip].append(now_ts)[m
[32m+[m[32m            ip_ports_seen[src_ip].add(dport)[m
[32m+[m[32m            ip_total_counts[src_ip] += 1[m
[32m+[m
     session = sessions[flow_key][m
     is_source = (src_ip == session["src_ip"] and sport == session["src_port"])[m
     pkt_len = len(packet)[m
[36m@@ -251,12 +278,17 @@[m [mdef process_packet(packet):[m
         elif session["syn_count"] > 0 and session["dpkts"] == 0:[m
             session["state"] = "INT" # Interrupted / No response (common in SYN floods)[m
 [m
[32m+[m[32m        # ── NEW: mark failed flows for failed_connection_ratio ──[m
[32m+[m[32m        if session["state"] in ("INT", "RST"):[m
[32m+[m[32m            with ip_lock:[m
[32m+[m[32m                ip_failed_counts[session["src_ip"]] += 1[m
[32m+[m
         # Calculate IAT (Inter-Arrival Times)[m
         iat_list = [t2 - t1 for t1, t2 in zip(session["timestamps"], session["timestamps"][1:])][m
         mean_iat = statistics.mean(iat_list) if iat_list else 0[m
[31m-        std_iat = statistics.stdev(iat_list) if len(iat_list) > 1 else 0[m
[32m+[m[32m        std_iat  = statistics.stdev(iat_list) if len(iat_list) > 1 else 0[m
         [m
[31m-        total_pkts = session["spkts"] + session["dpkts"][m
[32m+[m[32m        total_pkts  = session["spkts"] + session["dpkts"][m
         total_bytes = session["sbytes"] + session["dbytes"][m
         [m
         # Determine Service (Simple mapping by destination port)[m
[36m@@ -266,6 +298,52 @@[m [mdef process_packet(packet):[m
         elif session["src_port"] in SERVICE_MAP:[m
             service = SERVICE_MAP.get(session["src_port"], "-")[m
 [m
[32m+[m[32m        # ── NEW FEATURES ─────────────────────────────────────────────────────[m
[32m+[m
[32m+[m[32m        # 1. flow_bytes – total bytes exchanged in this flow (sbytes + dbytes)[m
[32m+[m[32m        flow_bytes = total_bytes[m
[32m+[m
[32m+[m[32m        # 2. bytes_per_pkt – average bytes per packet across the whole flow[m
[32m+[m[32m        bytes_per_pkt = (total_bytes / total_pkts) if total_pkts > 0 else 0[m
[32m+[m
[32m+[m[32m        # 3. syn_ratio – fraction of packets that carried a SYN flag[m
[32m+[m[32m        #    High values (near 1.0) indicate SYN-flood style behaviour[m
[32m+[m[32m        syn_ratio = (session["syn_count"] / total_pkts) if total_pkts > 0 else 0[m
[32m+[m
[32m+[m[32m        # 4. ack_ratio – fraction of packets that carried an ACK flag[m
[32m+[m[32m        #    Normal established flows hover around 1.0; very low values are suspicious[m
[32m+[m[32m        ack_ratio = (session["ack_count"] / total_pkts) if total_pkts > 0 else 0[m
[32m+[m
[32m+[m[32m        # 5. rst_ratio – fraction of packets that carried a RST flag[m
[32m+[m[32m        #    Elevated values suggest port-scanning or aggressive connection teardown[m
[32m+[m[32m        rst_ratio = (session["rst_count"] / total_pkts) if total_pkts > 0 else 0[m
[32m+[m
[32m+[m[32m        # 6. iat_ratio – coefficient of variation of inter-arrival times (std / mean)[m
[32m+[m[32m        #    High value → bursty / irregular traffic; low value → steady stream[m
[32m+[m[32m        iat_ratio = (std_iat / mean_iat) if mean_iat > 0 else 0[m
[32m+[m
[32m+[m[32m        # 7–9  Per-IP window metrics (thread-safe snapshot)[m
[32m+[m[32m        flow_src_ip = session["src_ip"][m
[32m+[m[32m        now_ts = now.timestamp()[m
[32m+[m[32m        with ip_lock:[m
[32m+[m[32m            _prune_window(flow_src_ip, now_ts)[m
[32m+[m
[32m+[m[32m            # 7. unique_ports_per_ip – distinct dst ports this src IP has[m
[32m+[m[32m            #    contacted since agent start (port-scan indicator)[m
[32m+[m[32m            unique_ports_per_ip = len(ip_ports_seen[flow_src_ip])[m
[32m+[m
[32m+[m[32m            # 8. connections_per_ip_window – how many connections this src IP[m
[32m+[m[32m            #    has opened inside the last IP_WINDOW_SECONDS seconds[m
[32m+[m[32m            connections_per_ip_window = len(ip_conn_times[flow_src_ip])[m
[32m+[m
[32m+[m[32m            # 9. failed_connection_ratio – ratio of failed (INT / RST) flows[m
[32m+[m[32m            #    to total flows for this src IP since agent start[m
[32m+[m[32m            total_for_ip  = ip_total_counts[flow_src_ip][m
[32m+[m[32m            failed_for_ip = ip_failed_counts[flow_src_ip][m
[32m+[m[32m            failed_connection_ratio = (failed_for_ip / total_for_ip) if total_for_ip > 0 else 0[m
[32m+[m
[32m+[m[32m        # ─────────────────────────────────────────────────────────────────────[m
[32m+[m
         final_log = {[m
             "hostname": session["hostname"],[m
             "timestamp": now.isoformat(),[m
[36m@@ -280,7 +358,7 @@[m [mdef process_packet(packet):[m
             "sttl": session["sttl"], "dttl": session["dttl"],[m
             "synack": session["synack"], "ackdat": session["ackdat"],[m
             "stcpb": session["stcpb"], "dtcpb": session["dtcpb"],[m
[31m-            # Engineered Features[m
[32m+[m[32m            # Original Engineered Features[m
             "rate": (total_bytes / duration) if duration > 0 else 0,[m
             "pkt_rate": (total_pkts / duration) if duration > 0 else 0,[m
             "byte_rate": (total_bytes / duration) if duration > 0 else 0,[m
[36m@@ -292,7 +370,18 @@[m [mdef process_packet(packet):[m
             "mean_iat": mean_iat,[m
             "std_iat": std_iat,[m
             "flow_packets": total_pkts,[m
[31m-            "port_count": len(session["ports_seen"]), # Usually 1 per flow unless tracking source wide[m
[32m+[m[32m            "port_count": len(session["ports_seen"]),[m
[32m+[m[32m            # ── NEW Features ──────────────────────────────────────────────────[m
[32m+[m[32m            "flow_bytes": flow_bytes,                                # total bytes in flow[m
[32m+[m[32m            "bytes_per_pkt": bytes_per_pkt,                          # avg bytes / packet[m
[32m+[m[32m            "syn_ratio": syn_ratio,                                  # SYN pkts / total pkts[m
[32m+[m[32m            "ack_ratio": ack_ratio,                                  # ACK pkts / total pkts[m
[32m+[m[32m            "rst_ratio": rst_ratio,                                  # RST pkts / total pkts[m
[32m+[m[32m            "iat_ratio": iat_ratio,                                  # std_iat / mean_iat (CoV)[m
[32m+[m[32m            "unique_ports_per_ip": unique_ports_per_ip,              # distinct dst ports (src IP)[m
[32m+[m[32m            "connections_per_ip_window": connections_per_ip_window,  # conns in sliding window[m
[32m+[m[32m            "failed_connection_ratio": failed_connection_ratio,      # failed / total (src IP)[m
[32m+[m[32m            # ─────────────────────────────────────────────────────────────────[m
             "processed": False[m
         }[m
 [m
